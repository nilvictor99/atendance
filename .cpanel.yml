deployment:
    tasks:
        - export DEPLOYPATH="${CPANEL_DEPLOY_PATH:-/home/softcont/public_html/demo/}"
        - cd ./public
        - find . -type f ! -name "index.php" -exec sh -c 'mkdir -p "$DEPLOYPATH/$(dirname {})" && cp {} "$DEPLOYPATH/$(dirname {})"' \;
